#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char ned[] = "\"Programming is like building a multilingual puzzle";

	puts(ned);

	return (0);
}
